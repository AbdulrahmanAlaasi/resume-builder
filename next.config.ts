import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static export removed to support API routes for university integration.
  // See app/api/* and PHASE-5 docs.
};

export default nextConfig;
